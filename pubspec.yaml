name: universities_proj
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  riverpod: ^2.1.1
  flutter_riverpod: ^2.1.1
  http: ^0.13.5
  equatable: ^2.0.5
  url_launcher: ^6.1.6
  dartz: ^0.10.1
  image_picker: ^0.8.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  mockito: ^5.3.2
  build_runner: ^2.3.2

flutter:

  uses-material-design: true
